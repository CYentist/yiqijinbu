<%= simple_form_for [@course, @event, @post] do |f| %>
<div class="form-job">
  <%= f.input :content, :label => " 内容" %>
</div>
<%= f.submit "提交", class: "btn btn-primary", data: { disable_with:"Submitting..."} %>
<% end %>
